#include <stdio.h>
#include <string.h>

int main()
{
    int i,j,str1[300];
    char str[300];

    gets(str);
    L = strlen(str);

    for(i = 0,j=0;i < L;i+=2,j++){
        str1[j] = str[i] - '0';
    }
    str1[] = sort()

    return 0;
}
